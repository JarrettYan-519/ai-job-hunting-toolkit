# AI Job Hunting Toolkit · 提示词质量审查报告

| | |
|---|---|
| **审查对象** | [JarrettYan-519/ai-job-hunting-toolkit](https://github.com/JarrettYan-519/ai-job-hunting-toolkit) |
| **审查范围** | `job-tracker.md` · `resume-optimizer.md` · `interview-prep.md` |
| **审查日期** | 2026-06-15 |
| **审查维度** | 角色设定、任务边界、输出一致性、流程严谨性、鲁棒性、联动闭环、跨平台兼容 |

> 共发现 **23 处可优化点**，按影响优先级组织。每点格式为：**问题 → 建议**。
> 严重程度图例：🔴 高（影响正确性/可用性） · 🟡 中（影响体验/深度） · 🟢 低（打磨项）

---

## 一、跨文件通用问题（优先级最高）

这些是三个文件共有的结构性问题，修复一处即惠及全部：

### 1. 🔴「当前日期」是全局隐患
三个文件到处依赖日期——投递日期、面试日期、截止日期、"超过一周无进展"、"临近截止 3 天内"。但纯对话型 AI（ChatGPT/Kimi 等）**不一定知道今天是几号**，会编造或使用训练截止日期，导致所有时间判断失准。

**建议**：在每个文件开头加一条全局规则——
> 「涉及日期判断时，若不确定当前日期，必须先向用户确认"今天是几号"再计算；或要求用户在每条更新消息里附带日期。」

### 2. 🔴 统计口径未定义
`job-tracker.md` 的统计表里有"简历筛选通过率""面试转化率"，但**分母是什么没写**（面试转化率 = 面试数/投递数？还是 /筛选通过数？）。不同会话 AI 算出的结果会不一致，破坏核心数据可信度。

**建议**：在统计章节明确每个比率的分子/分母公式，例如：
- 简历筛选通过率 = 进入"待面试"及之后状态的投递数 / 总投递数
- 面试转化率 = 产生面试记录的投递数 / 总投递数

### 3. 🔴 首次交互无开场引导
用户粘贴提示词后，AI 该主动介绍能力、还是等用户输入？三个文件都没说。常见后果是 AI 憋出一句"好的我准备好了，请问有什么可以帮你"——很弱，拉低第一印象。

**建议**：每个文件加一段「首次响应模板」。例如 job-tracker 上来就问："你现在有几条在投的？我帮你把表建起来。"

### 4. 🔴 ID 体系 & 跨表关联校验缺失
面试记录、失败反馈都有"关联投递"字段指向投递 ID，但**没说用户给了一个不存在的 ID 怎么办**（报错？提示？自动新建？）。三个 ID 命名（纯数字 / `#1` / `IP-1`）分散在文中各处，没有集中说明。

**建议**：加一节"ID 规范"集中定义三套 ID 的含义与分配规则，并明确关联校验失败时的处理流程。

### 5. 🔴 状态机流转规则不清
投递状态有 10 个值（`已投递`→`Offer`→`已接受`…），但**没定义合法转换路径**：
- 「已拒」之后能否复活？
- 「已过」和「Offer」的区别是什么？
- 接受一个 Offer 后，其他进行中的投递怎么收尾？

**建议**：补一张状态流转说明 + 收尾规则（如接受 Offer 后，其余投递自动标记"已主动放弃"）。

---

## 二、`job-tracker.md`（13 KB · 最扎实，细节最多）

### 6. 🟡 重复模式检测的"判重粒度"未定义
"同一原因出现 ≥2 次"——是按 15 个标签判重，还是按更细的根因？按标签太粗（`[技术深度]` 几乎每次都中），按根因又没定义怎么聚类。

**建议**：说明按"标签 + 根因关键词"判重，并给出一个聚类示例。

### 7. 🟡 「复盘记录」和模拟面试的产出没真正写回改进计划
复盘提取了"待补知识点"，但只说"建议加入你的面试准备材料"——**没明确写入 `job-improvement-plan.md`**。改进计划这个闭环漏了一半入口。

**建议**：明确"复盘/模拟中发现的薄弱点 → 直接生成 IP 条目写入改进计划"。

### 8. 🟡 缺少删除/撤销操作
实际使用中"记错了想删一条""状态改错了想回退"是高频需求，但完全没有对应操作。

**建议**：补两个操作——「删除投递」「撤销最近一次状态更新」。

### 9. 🟡 「避坑清单」里"同类岗位"如何界定未说
说"展示同类岗位（同方向）的高频失败点"，但"同方向"靠什么判断？岗位名关键词？标签？AI 可能乱归类。

**建议**：给判定规则（按标签池匹配，或要求投递记录新增"岗位方向"字段）。

### 10. 🟢 数据隐私提示缺失
公司名、HR 原话反馈、薪资都是敏感信息，且这套机制会让 AI 持久化存储它们。

**建议**：加一句脱敏提醒（如"分享对话截图前请脱敏"），并说明数据只存在用户本地文件、不上传。

---

## 三、`resume-optimizer.md`（4 KB · 明显偏薄）

### 11. 🔴 匹配度评分 X/10 没有"锚点"
没说 9 分和 6 分的差别，AI 每次打分会漂移。

**建议**：给评分锚点——
- 9-10：几乎完美匹配
- 7-8：基本匹配，有 1-2 个关键缺口
- 5-6：部分匹配，缺口较多
- ≤4：严重不匹配

### 12. 🔴 「关键词策略」和「诚实第一」存在张力，执行边界模糊
"JD 高频术语应出现在简历中（前提是用户确实具备）"——这个"前提"AI 实际执行时容易失守，把"接触过"包装成"精通"。

**建议**：明确定义"具备"的红线——**做过项目 / 能讲清原理才算**；仅听过、装过环境、看过文档不算。

### 13. 🟡 「量化优先」可能诱发编造
"用数字替换形容词"，且"用户没提供就追问具体数据"——但校招生常常**真的没有量化数据**，追问压力下容易编。

**建议**：补一条——确实无数据时，改用"具体且可验证的定性描述"，**禁止编造数字**。

### 14. 🟡 缺少独立的「简历体检」环节
技能堆砌、项目流水账、缺结果导向、时间线断层——这些通用硬伤没有专门的检查清单，只在 JD 匹配里隐含。

**建议**：增加一个不依赖具体 JD 的「通用简历体检」前置环节。

---

## 四、`interview-prep.md`（5 KB · 框架好，内容薄）

### 15. 🟡 题库只有分类，没有"典型题 + 参考答案要点"
AI Agent 方向列了 6 大类，但每类只是名词罗列。相比 job-tracker 的详尽，这里偏薄——而题库恰恰是用户最想要的"干货"。

**建议**：每个方向至少给 2-3 道带"参考答案要点"的样题（few-shot 也能提升 AI 后续出题质量）。

### 16. 🟡 模拟面试缺难度自适应和节奏控制
"面试官行为准则"写得好，但没说**根据用户水平调整追问深度**（对小白追问太深会打击信心）；"技术面 45-60 分钟"没转化成题量。

**建议**：加两条——首轮先探底用户水平 → 动态调难度；明确"技术面约 5-7 题"。

### 17. 🟡 联动入口比另两个文件少
"前置加载"只提了加载八股文件和引用简历，**没提读取 `job-improvement-plan.md`**——这本该是最精准的出题来源（你的薄弱点 = 该练的题）。

**建议**：补一条——优先读取改进计划里的未完成 IP 条目，针对性出题。

### 18. 🟢 评估维度「区分度」对用户不友好
"是否体现了与初级候选人的差异"——用户自己很难据此自评。

**建议**：改成可操作描述，如"是否给出了中级以上才会提到的 trade-off / 踩坑 / 替代方案"。

### 19. 🟢 「最大缺点」等行为题缺具体应对策略
这是经典坑题，但只列了题目没给"答法框架"。

**建议**：补三要素——**真实 + 非致命 + 正在改进**。

---

## 五、整体工程化建议（Polish）

### 20. 🟢 README 没说明「功能分级」
基础分析所有平台可用，自动持久化需要支持文件写入的平台（Claude/带工具的）。这直接影响用户预期。

**建议**：在 README 显著位置标注功能分级。

### 21. 🟢 缺版本号 / CHANGELOG
会迭代的项目建议加 `v1.0` 和更新日志，方便用户知道用的是哪版。

### 22. 🟢 缺真实对话 demo / 截图
README 全是文字描述，转化率会打折。一段"记录失败反馈 → 自动生成改进计划"的实际对话 GIF 最有说服力。

### 23. 🟢 Agent 框架对比硬编码可能过时
题库提 LangChain/AutoGPT/CrewAI，但 2026 年框架格局可能已变。

**建议**：提示词注明"框架对比以最新生态为准，自行补充"，避免写死。

---

## 六、建议优先处理的 Top 5

如果只改 5 处，性价比最高的是：

| 优先级 | 改动 | 涉及文件 | 理由 |
|---|---|---|---|
| 1 | 加全局「当前日期」规则 | 三个文件 | 一行字，消除三个文件最普遍的正确性隐患 |
| 2 | 定义统计口径公式 | job-tracker | 直接决定核心数据可信度 |
| 3 | 三个文件各加「首次响应模板」 | 三个文件 | 第一印象，提升留存 |
| 4 | 补评分锚点 + 诚实红线 | resume-optimizer | 修复"鼓励包装/编造"的执行风险 |
| 5 | 补全联动闭环（复盘/模拟写回改进计划） | job-tracker + interview-prep | 让"三件套联动"真正成立 |

---

## 附：审查方法论

本次审查从以下维度评估提示词质量：

1. **角色设定** — 是否给 AI 清晰的身份
2. **任务边界** — 做什么、不做什么是否清楚
3. **输出一致性** — 格式是否稳定、可预期
4. **流程严谨性** — 步骤是否有歧义、边界情况是否覆盖
5. **鲁棒性** — 异常输入、缺失信息如何处理
6. **联动闭环** — 三件套之间的数据流是否完整
7. **跨平台兼容** — 在不同 AI 工具上的表现差异

> **总体评价**：这是一套**完成度远超平均水准**的提示词工程项目。`job-tracker.md` 的"失败反馈四步处理流程"和"好/坏分析对比样例"尤其出色——少见的"教 AI 怎么思考"而非"只规定输出格式"的提示词。主要短板集中在**统计严谨性、状态机定义、跨文件联动闭环**三处，均为补强型而非重写型问题。
